apiVersion: v1
kind: Service
metadata:
  name: stocks-service
  namespace: stocks
spec:
  selector:
    app: stocks-app
  type: NodePort 
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
